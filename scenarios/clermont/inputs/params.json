[
	{
		"info": "To handle the PT network analysis, you need to provide an input file named zonage.geojson with columns 'xxx_density' in item/km² for each category required in the computation of network coverage (eg. population, jobs, health_facilities, etc). \nCatchment radius (in meters) and CAPEX (in €/veh.km) must be filled in the PT links editor, to fit to each route_type. \nThe model returns network indicators as tables and GIS layers that can be displayed here."
	},
	{
		"category": "General",
		"params": [
			{
				"text": "mesh step size",
				"value": 0.005,
				"type": "Number",
				"units": "degrees",
				"hint": "mesh step size",
				"rules": [
					"required",
					"nonNegative"
				],
				"name": "step_size"
			},
			{
				"text": "use road network for analysis",
				"value": true,
				"type": "Boolean",
				"units": "none",
				"hint": "use road network to compute population mesh",
				"rules": [
					"required",
					"nonNegative"
				],
				"name": "use_road_network"
			},
			{
				"text": "factor basic day to year",
				"value": 300,
				"type": "Number",
				"units": "none",
				"hint": "factor to compute yearly indicators from daily basis",
				"rules": [
					"required",
					"nonNegative"
				],
				"name": "coef_day_to_year"
			}
		]
	},
	{
		"category": "catchment_radius",
		"params": [
			{
				"text": "bus",
				"value": 500,
				"type": "Number",
				"units": "metres",
				"hint": "bus catchment distance",
				"rules": [
					"required",
					"nonNegative"
				],
				"name": "bus"
			},
			{
				"text": "subway",
				"value": 800,
				"type": "Number",
				"units": "metres",
				"hint": "subway catchment distance",
				"rules": [
					"required",
					"nonNegative"
				],
				"name": "subway"
			},
			{
				"text": "rail",
				"value": 1000,
				"type": "Number",
				"units": "metres",
				"hint": "rail catchment distance",
				"rules": [
					"required",
					"nonNegative"
				],
				"name": "rail"
			}
		]
	},
	{
		"category": "footpaths",
		"params": [
			{
				"text": "max length",
				"value": 1000,
				"type": "Number",
				"units": "metres",
				"hint": "max length for a footpath (walk distance between stations)",
				"rules": [
					"required",
					"nonNegative"
				],
				"name": "max_length"
			},
			{
				"text": "walk speed",
				"value": 2.8,
				"type": "Number",
				"units": "km/h",
				"hint": "as the crow flight walking speed",
				"rules": [
					"required",
					"nonNegative"
				],
				"name": "speed"
			},
			{
				"text": "number of ntlegs",
				"value": 2,
				"type": "Number",
				"units": "int",
				"hint": "number of connection between the origin (or destination) and the footpaths",
				"rules": [
					"required",
					"nonNegative"
				],
				"name": "n_ntlegs"
			}
		]
	}
]